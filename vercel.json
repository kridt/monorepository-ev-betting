{
  "installCommand": "pnpm install",
  "buildCommand": "cd /vercel/path0 && pnpm --filter @ev-bets/shared run build && pnpm --filter @ev-bets/web run build",
  "outputDirectory": "apps/web/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
