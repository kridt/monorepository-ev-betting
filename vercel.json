{
  "installCommand": "pnpm install",
  "buildCommand": "cd packages/shared && pnpm run build && cd ../../apps/web && pnpm run build",
  "outputDirectory": "apps/web/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
